<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.spring.petclinic.mapper.vet.VetMapper">
    <select id="findAllVets" resultType="dev.spring.petclinic.model.vet.Vet">
        SELECT id, first_name, last_name, specialties
        FROM vets
    </select>
</mapper>
